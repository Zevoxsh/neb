# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_secure_database_password_here
DB_NAME=nebuladb

# JWT Configuration
# CRITICAL: Generate a strong random secret (min 32 characters)
# Example: openssl rand -hex 32
# NEVER use default values in production!
JWT_SECRET=GENERATE_RANDOM_SECRET_WITH_OPENSSL_RAND_HEX_32

# Server Configuration
PORT=3000
NODE_ENV=production
COOKIE_SECURE=true

# Default Admin User (created on first startup)
# CRITICAL: Change these before first deployment!
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_NOW

# ACME/Let's Encrypt Configuration
ACME_EMAIL=your-email@example.com

# Optional: Local TLDs (comma-separated, no spaces)
LOCAL_TLDS=local,localhost,lan,test

# Optional: CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=

# Optional: Proxy Configuration
PROXY_PORT80_PUBLIC=true
BACKEND_FAILURE_THRESHOLD=3
BACKEND_FAILURE_COOLDOWN_MS=60000
BACKEND_CONNECT_TIMEOUT_MS=2000
BACKEND_HEALTH_INTERVAL_MS=30000

# Optional: Alert Configuration
ALERT_COOLDOWN_MS=900000

# Security: Bot Protection Secret (auto-generated if not set)
BOT_SECRET=

# Redis Configuration (optional - falls back to in-memory cache if unavailable)
REDIS_URL=redis://localhost:6379
REDIS_ENABLED=true

# HTTP Cache Configuration
HTTP_CACHE_ENABLED=false
HTTP_CACHE_DEFAULT_TTL=300

# WebSocket Configuration
WEBSOCKET_ENABLED=true
WEBSOCKET_PING_INTERVAL_MS=30000



QUESTION_1 = 